import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Renaissance Primary Palette ──────────────────────────────────────
  static const Color primary = Color(0xFF07372F);
  static const Color primaryDark = Color(0xFF0C2A22);
  static const Color secondary = Color(0xFFB28648);
  static const Color charcoal = Color(0xFF2E2A27);

  // ── Backgrounds ──────────────────────────────────────────────────────
  static const Color background = Color(0xFFF7F3EE);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFE7DCCF);

  // ── Neutrals ─────────────────────────────────────────────────────────
  static const Color neutral100 = Color(0xFFE7DCCF);
  static const Color neutral200 = Color(0xFFD9C8B4);
  static const Color neutral300 = Color(0xFFB7A693);
  static const Color neutral400 = Color(0xFFD8D4CE);

  // ── Text ─────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF2E2A27);
  static const Color textSecondary = Color(0xFF7A6F65);
  static const Color textMuted = Color(0xFFB7A693);
  static const Color textOnPrimary = Color(0xFFF7F3EE);
  static const Color textOnSecondary = Color(0xFFFFFFFF);

  // ── Semantic ─────────────────────────────────────────────────────────
  static const Color success = Color(0xFF2D6A4F);
  static const Color successLight = Color(0xFFD8F3DC);
  static const Color warning = Color(0xFFB28648);
  static const Color warningLight = Color(0xFFFFF3CD);
  static const Color error = Color(0xFF9B2226);
  static const Color errorLight = Color(0xFFFFE8E8);
  static const Color info = Color(0xFF1A5276);
  static const Color infoLight = Color(0xFFD6EAF8);

  // ── Priority ─────────────────────────────────────────────────────────
  static const Color priorityLow = Color(0xFF2D6A4F);
  static const Color priorityMedium = Color(0xFFB28648);
  static const Color priorityHigh = Color(0xFF9B2226);
  static const Color priorityUrgent = Color(0xFF6B0F1A);

  // ── Status ───────────────────────────────────────────────────────────
  static const Color statusPending = Color(0xFFB28648);
  static const Color statusApproved = Color(0xFF2D6A4F);
  static const Color statusRejected = Color(0xFF9B2226);
  static const Color statusInProgress = Color(0xFF1A5276);
}
